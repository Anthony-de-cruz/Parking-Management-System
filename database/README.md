The DB is to be run with `docker-compose`. To share used with either a shared volume with node or via TCP.
# Startup
### Unix
```sh
> docker-compose up
```
### Windows
Just use the Docker GUI
# Connect
Default IPv4: `0.0.0.0`, Port: `5432`